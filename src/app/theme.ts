import { breakpoints } from '../pkg/theme/breakpoints';
import { buttons } from '../pkg/theme/buttons';
import { palette } from '../pkg/theme/palette';
import { screens } from '../pkg/theme/screens';
import { sizes } from '../pkg/theme/sizes';
import { transitions } from '../pkg/theme/transitions';
import { typography } from '../pkg/theme/typography';
import { utils } from '../pkg/theme/utils';
import { zIndex } from '../pkg/theme/zIndex';

export const theme = {
  palette,
  typography,
  buttons,
  screens,
  breakpoints,
  utils,
  zIndex,
  sizes,
  transitions,
};
